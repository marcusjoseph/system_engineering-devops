#!/usr/bin/env bash
# This script displays the PID and process name of all processes containing "bash"

# List all PIDs and process names for processes matching "bash"
pgrep -a bash | awk '{print $1, $2}'
